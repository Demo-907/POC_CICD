name: key-pair-w-toml

on:
  push:
    branches:
      - main
    paths:
      - 'migrations/**'
  workflow_dispatch:

jobs:
  deploy-snowflake-changes-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Use Python 3.11.x
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Decrypt RSA key
        run: |
          echo "${{ secrets.SF_PRIVATE_KEY }}" > rsa_key.p8
          chmod 600 rsa_key.p8

      - name: Create connections.toml
        run: |
          echo "account = \"\${{ secrets.SF_ACCOUNT }}\"" >> .github/workflows/connections.toml
          echo "user = \"\${{ secrets.SF_USERNAME }}\"" >> .github/workflows/connections.toml
          echo "authenticator = \"SNOWFLAKE_JWT\"" >> .github/workflows/connections.toml
          echo "role = \"\${{ secrets.SF_ROLE }}\"" >> .github/workflows/connections.toml
          echo "warehouse = \"\${{ secrets.SF_WAREHOUSE }}\"" >> .github/workflows/connections.toml
          echo "database = \"\${{ secrets.SF_DATABASE }}\"" >> .github/workflows/connections.toml
          echo "private_key_file = \"rsa_key.p8\"" >> .github/workflows/connections.toml
          echo "private_key_file_pwd = \"\${{ secrets.SF_PRIVATE_KEY_PASSPHRASE }}\"" >> .github/workflows/connections.toml


      - name: List files in .github/workflows
        run: ls -l .github/workflows

      - name: Debug connections.toml
        run: cat .github/workflows/connections.toml

      - name: Set permissions for connections.toml
        run: chmod 0600 .github/workflows/connections.toml

      - name: Confirm connections.toml exists
        run: |
          if [ -f .github/workflows/connections.toml ]; then
            echo "✅ connections.toml exists"
          else
            echo "❌ connections.toml is missing"
            exit 1
          fi

      - name: Run schemachange
        run: |
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
          python --version
          echo "Step 1: Installing schemachange"
          pip install schemachange
          
          echo "Step 2: Running schemachange"
          schemachange -f sql-files --connections-file-path .github/workflows/connections.toml
       

      - name: Clean up sensitive files
        run: rm -f rsa_key.p8 .github/workflows/connections.toml
